spring.application.name=sarah-jewellers

# Database — uses env vars on Railway, falls back to localhost for dev
spring.datasource.url=${DATABASE_URL:jdbc:mysql://localhost:3306/sarah_jewellers?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=${DATABASE_USERNAME:root}
spring.datasource.password=${DATABASE_PASSWORD:Open05##}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Mail
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:tohazsha@gmail.com}
spring.mail.password=${MAIL_PASSWORD:namt drrw gkux bref}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# JWT
app.jwt.secret=${JWT_SECRET:SarahJewellersSecretKey2024SuperSecureKeyForJWT}
app.jwt.expiration=86400000

# File upload
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
app.upload.dir=uploads/

server.port=${PORT:8080}
